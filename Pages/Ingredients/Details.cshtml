@page
@model RecipeWebsite.Pages.Ingredients.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Szczegóły</h1>

<div>
    @*<h4>Ingredient</h4>*@
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Ingredient.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Ingredient.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Ingredient.TypeOfFood)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Ingredient.TypeOfFood)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayName("Przepisy")
        </dt>
        <dd class="col-sm-10">
            <table class="table">
                <tr>
                    <th>Nazwa</th>
                    <th>Ilość</th>
                    <th>Data ostatniego użycia</th>
                    <th></th>
                </tr>
                @foreach (var item in Model.Ingredient.QuantityInRecipes)
                {
            <tr>
                <td>
                    <a asp-page="../Recipes/Details" asp-route-id="@item.Recipe.RecipeID">@Html.DisplayFor(modelItem => item.Recipe.Name)</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Recipe.LastTimeServed)
                </td>
                <td>
                    <a asp-page="../QuantityInRecipes/Edit" asp-route-id="@item.QuantityInRecipeID">Edytuj ilość</a> |
                    <a asp-page="../QuantityInRecipes/Delete" asp-route-id="@item.QuantityInRecipeID">Usuń</a>
                </td>
            </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Ingredient.ID">Edytuj</a> |
    <a asp-page="./Index">Powrót</a> |
    <a asp-page="../QuantityInRecipes/Create">Dodaj składnik do przepisu</a>
</div>
